# إصلاح جدول أوامر الشغل

لقد حدث هذا الخطأ لأن قاعدة البيانات تفتقد لعمود مهم جداً (`executor_type`) في جدول `job_orders`، والذي يعتمد عليه النظام للتفرقة بين التنفيذ الذاتي ومقاولي الباطن.

يرجى نسخ الكود التالي وتشغيله في **SQL Editor** داخل لوحة تحكم **Supabase**:

```sql
-- إضافة العمود الناقص لجدول أوامر الشغل
ALTER TABLE public.job_orders 
ADD COLUMN IF NOT EXISTS executor_type text DEFAULT 'تنفيذ ذاتي';

-- تحديث الكاش لتطبيق التغييرات على الفور
NOTIFY pgrst, 'reload schema';
```

بعد تشغيل هذا الكود، ستتمكن من حفظ أوامر الشغل والصرف من المستودع بدون أي مشاكل! 🚀
